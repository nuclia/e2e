name: run-nuclia-e2e-prod
run-name: Running e2e PROD NUCLIA tests
on:
  push:
    branches:
      - '**'
    paths:
      - '.github/workflows/run-e2e-nuclia-prod.yml'  # Runs only if this file is modified
  workflow_dispatch:  # Allow manual run from any branch
  schedule:
    # Run every day at 04:25
    - cron: '25 4 * * *'
jobs:
  build-virtual-env:
    runs-on: ubuntu-22.04
    name: Build virtual environment
    permissions:
      contents: read    # actions/checkout
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0

      - name: Install venv
        run: uv sync --project nuclia_e2e

      - name: Upload virtualenv to cache
        uses: actions/cache/save@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: nuclia_e2e/.venv
          key: venv-${{ github.sha }}

  pre-checks:
    runs-on: ubuntu-22.04
    name: Python pre-checks
    permissions:
      contents: read    # actions/checkout
    needs:
      - build-virtual-env
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Restore venv
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: nuclia_e2e/.venv
          key: venv-${{ github.sha }}
          fail-on-cache-miss: true

      - name: Run pre-checks
        run: |
          source nuclia_e2e/.venv/bin/activate
          make -C nuclia_e2e/ lint format-check

  test:
    runs-on: ubuntu-22.04
    name: test-${{ matrix.zone }}_#${{ matrix.shard_index }}
    continue-on-error: true
    permissions:
      contents: read    # actions/checkout
      checks: write     # mikepenz/action-junit-report
    strategy:
      fail-fast: false
      matrix:
        zone: [gke-prod-1, aws-us-east-2-1, aws-il-central-1-1, aws-eu-central-1-1, aws-ap-southeast-2-1] # Remove aws-me-central-1-1 due to rocket issue
        shard_index: [0, 1, 2]  # Running tests in 3 parallel shards
    needs:
      - build-virtual-env
      - pre-checks

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Restore venv
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: nuclia_e2e/.venv
          key: venv-${{ github.sha }}
          fail-on-cache-miss: true

      - name: Log env (base64)
        run: |
          {
          echo "export PROD_PERMAMENT_ACCOUNT_OWNER_PAT_TOKEN=${{ secrets.PROD_PERMAMENT_ACCOUNT_OWNER_PAT_TOKEN }}"
          echo "export PROD_ROOT_PAT_TOKEN=${{ secrets.PROD_ROOT_PAT_TOKEN }}"
          echo "export PROD_GLOBAL_RECAPTCHA=${{ secrets.PROD_GLOBAL_RECAPTCHA }}"
          echo "export PROD_GCP_EUROPE1_NUA=${{ secrets.PROD_GCP_EUROPE1_NUA }}"
          echo "export PROD_AWS_US_EAST_2_1_NUA=${{ secrets.PROD_AWS_US_EAST_2_1_NUA }}"
          echo "export PROD_AWS_IL_CENTRAL_1_1_NUA=${{ secrets.PROD_AWS_IL_CENTRAL_1_1_NUA }}"
          echo "export PROD_AWS_EU_CENTRAL_1_1_NUA=${{ secrets.PROD_AWS_EU_CENTRAL_1_1_NUA }}"
          echo "export PROD_AWS_ME_CENTRAL_1_1_NUA=${{ secrets.PROD_AWS_ME_CENTRAL_1_1_NUA }}"
          echo "export PROD_AWS_AP_SOUTHEAST_2_1_NUA=${{ secrets.PROD_AWS_AP_SOUTHEAST_2_1_NUA }}"
          echo "export TEST_GMAIL_APP_PASSWORD=${{ secrets.TEST_GMAIL_APP_PASSWORD }}"
          echo "export GOOGLE_DRIVE_CLIENT_SECRET=${{ secrets.GOOGLE_DRIVE_CLIENT_SECRET }}"
          echo "export GOOGLE_DRIVE_REFRESH_TOKEN=${{ secrets.GOOGLE_DRIVE_REFRESH_TOKEN }}"
          echo "export AZURE_CLIENT_SECRET=${{ secrets.AZURE_CLIENT_SECRET }}"
          echo "export AZURE_REFRESH_TOKEN=${{ secrets.AZURE_REFRESH_TOKEN }}"
          echo "export GRAFANA_URL=${{ secrets.GRAFANA_URL }}"
          echo "export TEST_ZONES=${{ matrix.zone }}"
          echo "export TEST_ENV=prod"
          } | gzip -c | base64 -w 80

      - name: "Run tests"
        env: # Or as an environment variable
          PROD_PERMAMENT_ACCOUNT_OWNER_PAT_TOKEN: ${{ secrets.PROD_PERMAMENT_ACCOUNT_OWNER_PAT_TOKEN }}
          PROD_ROOT_PAT_TOKEN: ${{ secrets.PROD_ROOT_PAT_TOKEN }}
          PROD_GLOBAL_RECAPTCHA: ${{ secrets.PROD_GLOBAL_RECAPTCHA }}
          PROD_GCP_EUROPE1_NUA: ${{ secrets.PROD_GCP_EUROPE1_NUA }}
          PROD_AWS_US_EAST_2_1_NUA: ${{ secrets.PROD_AWS_US_EAST_2_1_NUA }}
          PROD_AWS_IL_CENTRAL_1_1_NUA: ${{ secrets.PROD_AWS_IL_CENTRAL_1_1_NUA }}
          PROD_AWS_EU_CENTRAL_1_1_NUA: ${{ secrets.PROD_AWS_EU_CENTRAL_1_1_NUA }}
          PROD_AWS_ME_CENTRAL_1_1_NUA: ${{ secrets.PROD_AWS_ME_CENTRAL_1_1_NUA }}
          PROD_AWS_AP_SOUTHEAST_2_1_NUA: ${{ secrets.PROD_AWS_AP_SOUTHEAST_2_1_NUA }}
          TEST_GMAIL_APP_PASSWORD: ${{ secrets.TEST_GMAIL_APP_PASSWORD }}
          GOOGLE_DRIVE_CLIENT_SECRET: ${{ secrets.GOOGLE_DRIVE_CLIENT_SECRET }}
          GOOGLE_DRIVE_REFRESH_TOKEN: ${{ secrets.GOOGLE_DRIVE_REFRESH_TOKEN }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          AZURE_REFRESH_TOKEN: ${{ secrets.AZURE_REFRESH_TOKEN }}
          GRAFANA_URL: ${{ secrets.GRAFANA_URL }}
          TEST_ZONES: ${{ matrix.zone }}
          TEST_ENV: "prod"

        run: |
          source nuclia_e2e/.venv/bin/activate && pytest -sxvr nuclia_e2e/nuclia_e2e/tests \
            --durations=0 --junitxml=nuclia-${{ matrix.shard_index }}.xml \
            --shard-id=${{ matrix.shard_index }} --num-shards=3

      - name: Mark failure for zone
        if: failure()
        run: echo "${{ matrix.zone }}" > failed_zone.txt

      - name: Upload failed zone (if failed)
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: failed-zone-${{ matrix.zone }}-${{ matrix.shard_index }}
          path: failed_zone.txt
      - name: Publish Test Report
        uses: mikepenz/action-junit-report@3a81627bfac62268172037048872e8ebd4207e6d # v6.4.1
        if: success() || failure() # always run even if the previous step fails
        with:
          report_paths: 'nuclia-*.xml'


  notify:
    runs-on: ubuntu-latest
    permissions: {}      # no token needed; Slack webhook uses a secret
    needs: test
    if: always()  # Always run to check if any zone failed
    steps:
      - name: Download all failure artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: all-failed-zones

      - name: Collect unique failed zones
        id: collect
        run: |
          if [ -d all-failed-zones ]; then
            failed_zones=$(cat all-failed-zones/**/failed_zone.txt | sort | uniq | tr '\n' ',' | sed 's/,$//')
            echo "failed_zones=$failed_zones" >> $GITHUB_OUTPUT
          else
            echo "failed_zones=" >> $GITHUB_OUTPUT
          fi

      - name: Send Slack Notification
        if: ${{ steps.collect.outputs.failed_zones != '' && github.ref_name == 'main' }}
        uses: slackapi/slack-github-action@45a88b9581bfab2566dc881e2cd66d334e621e2c # v3.0.3
        with:
          webhook-type: incoming-webhook
          webhook: ${{ secrets.NUA_E2E_SLACK_WEBHOOK }}
          payload: |
            {
              "text": "⚠️ Tests failed in zones: ${{ steps.collect.outputs.failed_zones }}\n${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            }

      # Always run to check if any zone failed. THis is needed because otherwise the `continue-on-error: true on the test config would make it always a success`
      - name: Mark workflow as failed if any zone failed
        if: ${{ steps.collect.outputs.failed_zones != '' }}
        run: |
          echo "One or more test zones failed: ${STEPS_COLLECT_OUTPUTS_FAILED_ZONES}"
          exit 1
        env:
          STEPS_COLLECT_OUTPUTS_FAILED_ZONES: ${{ steps.collect.outputs.failed_zones }}
